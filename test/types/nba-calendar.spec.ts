import { createCalendar } from '../../src/types/nba-calendar';

describe('NbaCalendar', () => {
  describe('createCalendar()', () => {
    test('should exist', () => expect(createCalendar).toBeDefined());
  });
});
