import { createNbaGame } from '../../src/types/nba-game';

describe('NbaCalendar', () => {
  describe('createNbaGame()', () => {
    test('should exist', () => expect(createNbaGame).toBeDefined());
  });
});
