import { createNbaPlayByPlay } from '../../src/types/nba-play-by-play';

describe('NbaPlayByPlay', () => {
  describe('createNbaPlayByPlay()', () => {
    test('should exist', () => expect(createNbaPlayByPlay).toBeDefined());
  });
});
