import { createNbaPlayerProfile } from '../../src/types/nba-player-profile';

describe('NbaPlayerProfile', () => {
  describe('createNbaPlayerProfile()', () => {
    test('should exist', () => expect(createNbaPlayerProfile).toBeDefined());
  });
});
